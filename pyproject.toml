[project]
name = "ep-ia-mlp-cnn"
version = "0.1.0"
description = "Exercicio Programa IA: MLP + CNN"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
    "ruff>=0.11",
    "pre-commit>=4.0",
]
